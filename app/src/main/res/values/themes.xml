<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.IncpenseTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:colorPrimary">@color/appColor</item>

        //status bar
        <item name="android:statusBarColor">@color/appColor</item>

    </style>

    <style name="Theme.IncpenseTracker" parent="Base.Theme.IncpenseTracker" />

    <style name="bottomNavigationBar" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/appColorDark</item>
    </style>

</resources>